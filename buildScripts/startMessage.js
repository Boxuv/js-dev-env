// Chalk is a library that allows to specify a color of the output to the console

//ES 5:
//var chalk = require('chalk');

//ES 6:
import chalk from 'chalk';

console.log(chalk.green('Starting up the app in DEV MODE'));
