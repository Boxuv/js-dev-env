import './index.css';
//import numeral from 'numeral';

//const courseValue = numeral(1000).format('$0,0.00');

// 'debugger;' to use sourcemapping in the browser, to see source code and not illegible bundled code
//debugger;
//console.log(`Id pay ${courseValue} for this`);
